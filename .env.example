# Fixtures boot and run without credentials.
DATA_SOURCE=fixture
TENANT_ID=demo-tenant
FIXTURE_PATH=./data/fixtures/accounts.json
MASTRA_DB_URL=file:./mastra.db
TURSO_AUTH_TOKEN=
CUSTOMER_SUCCESS_CRON=0 9 * * 1
CUSTOMER_SUCCESS_TIMEZONE=UTC

# Only needed when chatting with the agent; the fixture workflow is deterministic.
MODEL=openai/gpt-5-mini
GENERATION_MODE=deterministic
MODEL_INPUT_COST_PER_MILLION=0
MODEL_OUTPUT_COST_PER_MILLION=0
OPENAI_API_KEY=
ENABLE_OBSERVATIONAL_MEMORY=false
ENABLE_SEMANTIC_RECALL=false
EMBEDDING_MODEL=openai/text-embedding-3-small

# Optional normalized product-usage endpoint.
SIGNALS_API_URL=
SIGNALS_API_TOKEN=

# HubSpot reads companies, tickets, invoices, and feedback; it writes notes and tasks.
HUBSPOT_PRIVATE_APP_TOKEN=
HUBSPOT_BASE_URL=https://api.hubapi.com
HUBSPOT_RENEWAL_PROPERTY=renewal_date
MAX_ACCOUNT_CONCURRENCY=4
